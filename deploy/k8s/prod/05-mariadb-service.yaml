apiVersion: v1
kind: Service
metadata:
  name: mariadb-svc
  namespace: ddts-prod
  labels:
    app: mariadb
    environment: prod
spec:
  type: ClusterIP
  selector:
    app: mariadb
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
