apiVersion: v1
kind: Service
metadata:
  name: mariadb-svc
  namespace: ddts-staging
  labels:
    app: mariadb
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: mariadb
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
